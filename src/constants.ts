type UnitMap = Record<string, string>;

export const WEIGHT_UNITS: UnitMap = {
  'imperial': 'lbs',
  'metric': 'kg',
};

export const HEIGHT_UNITS: UnitMap = {
  'imperial': 'ft',
  'metric': 'm',
};

export const UNITS = ['imperial', 'metric'];